# Files in this repo that should NOT be served on the public website.
# (The HTML/CSS/font/image assets are served; planning docs and config are not.)
*.md
**/*.md
wrangler.jsonc
.assetsignore
.DS_Store
**/.DS_Store
CNAME
social-media
# Reference copies for the Claude Desktop project. Tracked in git so they
# follow between machines, but never served -- otherwise the .html copies
# would appear as duplicate pages at /desktop-sync/.
desktop-sync

